import { View, Text } from 'react-native';
import React, { FC } from 'react';

const NativeWindDemo: FC = () => {
  return (
    <View style={{ flex: 1, justifyContent: 'center', alignItems: 'center' }}>
      <Text>NativeWindDemo</Text>
    </View>
  );
};

export default NativeWindDemo;
